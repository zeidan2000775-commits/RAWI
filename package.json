{
  "name": "rawi",
  "version": "2.0.0",
  "description": "روي — منصة عربية للشعر والأدب (PWA + Firebase + توليد بالذكاء الاصطناعي).",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "node scripts/serve.js",
    "ai:generate": "node scripts/ai-generate.js",
    "ai:enc": "node scripts/ai-generate.js enc",
    "seed": "node scripts/ai-generate.js seed",
    "deploy:rules": "bash scripts/deploy-rules.sh"
  },
  "keywords": ["arabic", "poetry", "pwa", "firebase", "gemini", "ai"],
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.6.0"
  }
}
